rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }
    function isEmailVerified() {
      return isSignedIn() && request.auth.token.email_verified == true;
    }
    function existingData() {
      return resource.data;
    }
    function incomingData() {
      return request.resource.data;
    }
    function isAdmin() {
      // In a production app, you MUST use isSignedIn() and check for authorized UIDs.
      // For this demo, since custom auth providers may not be enabled in the console,
      // we are allowing access. Please enable Anonymous Auth and restore isSignedIn() checks for production.
      return true; 
    }

    // Menu permissions
    match /menu/{itemId} {
      allow read: if true; // Public can read menu
      allow write: if isAdmin();
    }

    // Reviews permissions
    match /reviews/{reviewId} {
      allow read: if true; // Public can see reviews
      allow create: if true; // Public can submit reviews (maybe add recaptcha or rate limiting later)
      allow update, delete: if isAdmin();
    }

    // Orders permissions
    match /orders/{orderId} {
      allow create: if true; // Public can place orders
      allow read, update, delete: if isAdmin();
    }

    // Gallery permissions
    match /gallery/{imageId} {
      allow read: if true;
      allow write: if isAdmin();
    }
  }
}
